// Bump this filename whenever the logo image is replaced (rename the new
// file in /public to match) so browsers don't serve a stale cached copy
// from the old file. Next.js's image optimizer blocks query-string cache
// busting on local images by default, so the version has to live in the
// filename itself.
export const LOGO_PATH = "/logo-v4.png";
